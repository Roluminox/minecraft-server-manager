# =============================================================================
# CONFIGURATION SERVEUR MINECRAFT
# =============================================================================
# Copie ce fichier en .env et modifie les valeurs selon tes besoins
# cp .env.example .env (Linux) ou copy .env.example .env (Windows)
# =============================================================================

# --- IMAGE DOCKER ---
# Tags disponibles: java21, java17, java8, latest
# Voir: https://hub.docker.com/r/itzg/minecraft-server/tags
MC_IMAGE_TAG=java21

# --- VERSION MINECRAFT ---
# LATEST = dernière stable, ou spécifie une version (1.21.5, 1.20.4, etc.)
MC_VERSION=1.21.5

# --- TYPE DE SERVEUR ---
# VANILLA, PAPER, SPIGOT, FABRIC, FORGE, etc.
# Pour les mods: FABRIC ou FORGE
# Pour les plugins: PAPER ou SPIGOT
MC_TYPE=VANILLA

# --- MÉMOIRE ---
# RAM allouée au serveur Minecraft (pas au container)
# Recommandé: 4G pour 2-5 joueurs, 6G pour 6-10 joueurs
MC_MEMORY=4G

# --- LIMITES RESSOURCES CONTAINER ---
# CPU max (nombre de coeurs)
MC_CPU_LIMIT=4.0
# RAM max container (doit être > MC_MEMORY + 1G pour la JVM)
MC_MEM_LIMIT=6G

# --- GAMEPLAY ---
MC_MAX_PLAYERS=10
MC_MOTD=Serveur Minecraft entre amis
MC_DIFFICULTY=normal
# survival, creative, adventure, spectator
MC_MODE=survival
MC_PVP=true
MC_COMMAND_BLOCK=false
MC_SPAWN_PROTECTION=0
MC_VIEW_DISTANCE=10

# --- SÉCURITÉ ---
# Whitelist: FORTEMENT recommandé pour serveur privé
MC_WHITELIST=true
MC_ENFORCE_WHITELIST=true

# --- RÉSEAU (Option 2 uniquement) ---
# Port local (doit correspondre au port-forwarding de ta box)
MC_PORT=25565

# --- SYSTÈME ---
# Timezone (pour les logs)
TZ=Europe/Paris

# User/Group ID (Linux uniquement, ignoré sur Windows)
# Utilise `id -u` et `id -g` pour trouver tes IDs
PUID=1000
PGID=1000

# =============================================================================
# POUR ADAPTER À UN AUTRE JEU
# =============================================================================
# 1. Change l'image dans docker-compose.yml
# 2. Adapte les variables d'environnement
# 3. Change le port (ex: Valheim=2456-2458, Palworld=8211)
#
# Exemples d'images populaires:
# - Valheim: lloesche/valheim-server
# - Palworld: thijsvanloef/palworld-server-docker
# - Terraria: ryshe/terraria
# - Factorio: factoriotools/factorio
# - 7 Days to Die: vinanrra/7dtd-server
# =============================================================================
